<main class="main user-forgot-password-page">
    <div class="page-header">
        <div class="container">
            <h1 class="page-title mb-0">User Password Reset</h1>
        </div>
    </div>

    <div class="page-content mb-10">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-6 col-lg-5">
                    <div class="user-login-popup mt-5">
                        <div class="tab tab-nav-boxed tab-nav-center tab-nav-underline">
                            <div class="tab-content">
                                <% if (messages.error) { %>
                                    <div class="alert alert-danger">
                                        <%= messages.error %>
                                    </div>
                                <% } %>
                                <% if (messages.success) { %>
                                    <div class="alert alert-success">
                                        <%= messages.success %>
                                    </div>
                                <% } %>
                                <form action="/auth/user/forgot-password" method="POST" class="mt-4">
                                    <div class="form-group">
                                        <label>Email Address *</label>
                                        <input type="email" class="form-control" name="email" required>
                                    </div>
                                    <button type="submit" class="btn btn-primary">Reset User Password</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
